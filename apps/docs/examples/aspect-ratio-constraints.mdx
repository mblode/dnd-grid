---
title: Aspect ratio constraints
description: Lock item resizing to a ratio.
---

<iframe
  src="https://dnd-grid.com/examples/aspect-ratio-constraints?embed=true"
  width="100%"
  height="460"
  style={{ border: "1px solid #e5e7eb", borderRadius: "8px", marginBottom: "24px" }}
/>

## Code

```tsx
import { useState } from "react";
import { DndGrid, type Layout, aspectRatio } from "@dnd-grid/react";
import "@dnd-grid/react/styles.css";

const ratio16x9 = aspectRatio(16 / 9);
const ratio1x1 = aspectRatio(1);

const initialLayout: Layout = [
  { i: "16:9", x: 0, y: 0, w: 6, h: 3, constraints: [ratio16x9] },
  { i: "1:1", x: 6, y: 0, w: 3, h: 3, constraints: [ratio1x1] },
  { i: "free", x: 9, y: 0, w: 3, h: 2 },
];

export default function AspectRatioConstraintsExample() {
  const [layout, setLayout] = useState<Layout>(initialLayout);

  return (
    <DndGrid
      layout={layout}
      cols={12}
      rowHeight={50}
      width={1200}
      onLayoutChange={setLayout}
    >
      {layout.map((item) => (
        <div key={item.i} className="grid-item">
          {item.i}
        </div>
      ))}
    </DndGrid>
  );
}
```
