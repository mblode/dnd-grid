---
title: Static items
description: Static items cannot be moved or resized.
---

<iframe
  src="https://dnd-grid.com/examples/static-elements?embed=true"
  width="100%"
  height="450"
  style={{ border: "1px solid #e5e7eb", borderRadius: "8px", marginBottom: "24px" }}
/>

## Code

```tsx
import { useState } from "react";
import { DndGrid, type Layout } from "@dnd-grid/react";
import "@dnd-grid/react/styles.css";

const initialLayout: Layout = [
  { i: "0", x: 0, y: 0, w: 3, h: 2, static: true },
  { i: "1", x: 3, y: 0, w: 3, h: 2 },
  { i: "2", x: 6, y: 0, w: 3, h: 2, static: true },
  { i: "3", x: 9, y: 0, w: 3, h: 2 },
];

export default function StaticElementsExample() {
  const [layout, setLayout] = useState<Layout>(initialLayout);

  return (
    <DndGrid
      layout={layout}
      cols={12}
      rowHeight={50}
      width={1200}
      onLayoutChange={setLayout}
    >
      {layout.map((item) => (
        <div key={item.i} className={item.static ? "static-item" : "grid-item"}>
          {item.i} {item.static ? "(static)" : ""}
        </div>
      ))}
    </DndGrid>
  );
}
```
