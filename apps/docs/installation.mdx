---
title: Installation
description: Install and configure dnd-grid.
---

## Setup

Install the package and import the required styles.

<CodeGroup>

```bash npm
npm install @dnd-grid/react
```

```bash yarn
yarn add @dnd-grid/react
```

```bash pnpm
pnpm add @dnd-grid/react
```

</CodeGroup>

```tsx
import "@dnd-grid/react/styles.css";
```

<Note>
  **TypeScript:** Type definitions are included automatically. No `@types` package is needed.
</Note>
