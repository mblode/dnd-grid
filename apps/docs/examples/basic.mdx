---
title: Basic
description: Simple draggable and resizable grid layout.
---

<iframe
  src="https://dnd-grid.com/examples/basic?embed=true"
  width="100%"
  height="450"
  style={{ border: "1px solid #e5e7eb", borderRadius: "8px", marginBottom: "24px" }}
/>

## Code

```tsx
import { useState } from "react";
import { DndGrid, type Layout } from "@dnd-grid/react";
import "@dnd-grid/react/styles.css";

const generateLayout = (count: number): Layout => {
  return Array.from({ length: count }, (_, i) => ({
    i: i.toString(),
    x: (i * 2) % 12,
    y: Math.floor(i / 6) * 2,
    w: 2,
    h: 2,
  }));
};

export default function BasicExample() {
  const [layout, setLayout] = useState<Layout>(generateLayout(12));

  return (
    <DndGrid
      layout={layout}
      cols={12}
      rowHeight={50}
      width={1200}
      onLayoutChange={setLayout}
    >
      {layout.map((item) => (
        <div key={item.i} className="grid-item">
          {item.i}
        </div>
      ))}
    </DndGrid>
  );
}
```
