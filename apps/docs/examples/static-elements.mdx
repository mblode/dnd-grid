---
title: Static items
description: Static items cannot be moved or resized.
---

<div className="not-prose my-6 rounded-lg border border-zinc-200/70 bg-white/70 shadow-sm dark:border-white/10 dark:bg-white/5">
  <iframe
    title="Static items preview"
    src="https://dnd-grid.com/examples/static-elements-example?embed=1"
    className="h-[640px] w-full"
    loading="lazy"
  />
</div>

[View source on GitHub](https://github.com/mblode/dnd-grid/blob/main/apps/web/examples/dnd-grid-static-elements-example.tsx)

## Installation

<Tabs>
<Tab title="CLI">

```bash
npx shadcn@latest add https://dnd-grid.com/r/static-elements-example.json
```

</Tab>
<Tab title="Manual">

```bash
npm install @dnd-grid/react
```

```css
@import "@dnd-grid/react/styles.css";
```

```tsx title="components/dnd-grid-static-elements-example.tsx"
"use client";

import { DndGrid, type Layout } from "@dnd-grid/react";
import { useState } from "react";

const initialLayout: Layout = [
  { id: "0", x: 0, y: 0, w: 3, h: 2, static: true },
  { id: "1", x: 3, y: 0, w: 3, h: 2 },
  { id: "2", x: 6, y: 0, w: 3, h: 2, static: true },
  { id: "3", x: 9, y: 0, w: 3, h: 2 },
];

export function StaticElementsExample() {
  const [layout, setLayout] = useState<Layout>(initialLayout);

  return (
    <DndGrid
      layout={layout}
      cols={12}
      rowHeight={50}
      onLayoutChange={setLayout}
    >
      {layout.map((item) => (
        <div
          key={item.id}
          className={item.static ? "static-item" : "grid-item"}
        >
          {item.id} {item.static ? "(static)" : ""}
        </div>
      ))}
    </DndGrid>
  );
}
```

</Tab>
</Tabs>

## Usage

```tsx
import { StaticElementsExample } from "@/components/dnd-grid-static-elements-example";

export default function Page() {
  return <StaticElementsExample />;
}
```






